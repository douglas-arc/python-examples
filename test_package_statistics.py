import unittest
from package_statistics import top_packages

class TestPackageStats(unittest.TestCase):
    def test_package_stats(self):
        # self.assertEqual(top_packages('arm64'), correct-value) 
        # self.assertEqual(top_packages('amd64'), correct-value)


if __name__ == '__name__':
    unittest.main()